{
  "name": "site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "dianome.dev: home, /summarize (Phase 7 vertical demo), /stats (load + session dashboards), /writeup",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "dianome": "workspace:*",
    "dianome-runtime": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.0.0",
    "@webgpu/types": "^0.1.60",
    "marked": "^17.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
